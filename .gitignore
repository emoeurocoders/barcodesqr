# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Claude Code, machine-local. The auto-commit hook and its kill switch are
# per-checkout choices, not repo policy — unlike .githooks/, which IS committed
# so the wrong-author guard travels with the repo.
.claude/settings.local.json
.claude/auto-commit.sh
.claude/no-auto-commit
